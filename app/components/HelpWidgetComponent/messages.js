/*
 * HelpWidgetComponent Messages
 *
 * This contains all the text for the HelpWidgetComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HelpWidgetComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HelpWidgetComponent component!',
  },
});
