/*
 * FrequentlyAskedHelperWidget Messages
 *
 * This contains all the text for the FrequentlyAskedHelperWidget component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FrequentlyAskedHelperWidget';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FrequentlyAskedHelperWidget component!',
  },
});
